{
  "name": "deepdesk",
  "version": "0.1.0",
  "description": "DeepDesk — AI research desk that pays its own way. Gemini-powered research agent with its own USDC wallet.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "express": "^4.21.0"
  }
}
